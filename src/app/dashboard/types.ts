export type PassType = {
    id: number
    desc: string
    createdAt: Date
}

export type CardType = {
    id: number
    desc: string
    holderName: string
    cardNumber: string
    cvv: string
    createdAt: Date
}
